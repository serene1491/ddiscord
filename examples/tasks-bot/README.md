# Tasks Bot

This console focuses on the built-in task scheduler.

It demonstrates:

- scheduling one-shot reminders from commands
- recurring scheduler heartbeats
- lightweight state tracking for task usage

## Run

```sh
cd examples/tasks-bot
dub run
```

Try:

- `!timer`
- `!timer 10`
- `!timers`
